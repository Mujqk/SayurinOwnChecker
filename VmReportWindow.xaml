<Window x:Class="BazaChecker.VmReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Отчет о виртуализации" Height="600" Width="800"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Border Background="#050508" CornerRadius="12" BorderBrush="#3A1D54" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="#101015" CornerRadius="12,12,0,0" MouseDown="TitleBar_MouseDown" BorderBrush="#3A1D54" BorderThickness="0,0,0,1">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                        <TextBlock Text="🛡️" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="White"/>
                        <TextBlock Text="Отчет о виртуализации" Foreground="White" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Center" Width="36" Height="36" Margin="0,0,15,0"
                            Background="Transparent" Foreground="#888" BorderThickness="0" Click="Close_Click" Cursor="Hand" FontSize="16">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="bd" Background="Transparent" CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bd" Property="Background" Value="#EF4444"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Log Content -->
            <Border Grid.Row="1" Background="#0A0A0F" Margin="15" CornerRadius="8" BorderBrush="#3A1D54" BorderThickness="1">
                <RichTextBox Name="ConsoleOutput" Background="Transparent" Foreground="#D1D5DB" FontFamily="Consolas" FontSize="13" 
                             BorderThickness="0" IsReadOnly="True" VerticalScrollBarVisibility="Auto" Padding="10"/>
            </Border>

            <!-- Footer -->
            <Border Grid.Row="2" Background="#101015" CornerRadius="0,0,12,12" BorderBrush="#3A1D54" BorderThickness="0,1,0,0">
                <Button Content="Закрыть" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,20,0"
                        Width="120" Height="36" Click="Close_Click" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#3B82F6"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="bd" Property="Background" Value="#2563EB"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Border>
        </Grid>
    </Border>
</Window>
